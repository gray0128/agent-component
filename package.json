{
  "name": "agent-component-monorepo",
  "private": true,
  "scripts": {
    "dev:core": "pnpm --filter @agent-component/core dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
